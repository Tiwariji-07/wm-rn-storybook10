# Callback Events

The Spinner component does not expose any specific callback events. Spinner visibility is typically controlled through the `show` property or by tracking variables that automatically manage the spinner's display state.