# Callback Events

| Event | Description |
|-------|-------------|
| `onSelect` | Triggered when a navigation item is selected. Receives event object, target element, and selected item data |

The `onSelect` event provides access to the selected navigation item and can be used to implement custom navigation logic, analytics tracking, or conditional navigation based on user permissions.