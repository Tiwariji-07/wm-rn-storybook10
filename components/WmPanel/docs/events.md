# Callback Events

| Event | Description |
|-------|-------------|
| `onLoad` | Triggered when the panel component is fully loaded and initialized |
| `onTap` | Fired when the user taps on the panel header area |